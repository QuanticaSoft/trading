# Same PostgreSQL instance the backend writes to (read-only usage here)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=trading

# Backend base URL and the shared secret for POST /internal/metrics
BACKEND_URL=http://localhost:3000
METRICS_WEBHOOK_SECRET=
