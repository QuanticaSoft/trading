# Telegram bot token from https://t.me/BotFather — never commit the real value
BOT_TOKEN=

# Base URL of the NestJS backend's internal webhook endpoints
BACKEND_URL=http://localhost:3000

# Shared secret sent in a header on every backend call, so the backend can
# reject requests that didn't come from this bot
BACKEND_WEBHOOK_SECRET=
