PORT=3000
NODE_ENV=development

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=trading

# Must match BACKEND_WEBHOOK_SECRET in bot/.env.example — never commit the real value
BOT_WEBHOOK_SECRET=

# Must match METRICS_WEBHOOK_SECRET in metrics-service/.env.example
METRICS_WEBHOOK_SECRET=
